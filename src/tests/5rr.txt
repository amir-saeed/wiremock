raw_df.filter(
    (F.col("dal_year") == F.lit(str(reporting_date.year)))
    & (F.col("dal_month") == F.lit(f"{reporting_date.month:02d}"))
    & (F.col("dal_day") == F.lit(f"{reporting_date.day:02d}"))
)